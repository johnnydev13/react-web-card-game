import BaseController from "./BaseController";

export default class SystemController extends BaseController{
    constructor() {
        super();
    }

    actionDisconnect(client, data, callback) {
        // TODO make something when user disconnects
    }
}
